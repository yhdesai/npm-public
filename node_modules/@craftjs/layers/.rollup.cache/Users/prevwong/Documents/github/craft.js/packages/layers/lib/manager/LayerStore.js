import { Store } from '@craftjs/utils';
import { LayerMethods } from './actions';
export class LayerStore extends Store {
    get actions() {
        const methods = LayerMethods(null);
        return Object.keys(methods).reduce((accum, actionKey) => {
            return {
                ...accum,
                [actionKey]: (...args) => this.setState((state) => LayerMethods(state)[actionKey](...args)),
            };
        }, {});
    }
}
//# sourceMappingURL=LayerStore.js.map