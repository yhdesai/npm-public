import React from 'react';
import { EditorQuery } from 'store';
export declare function connectEditor<C>(collect?: (query: EditorQuery) => C): (WrappedComponent: React.ElementType<any>) => (props: any) => JSX.Element;
