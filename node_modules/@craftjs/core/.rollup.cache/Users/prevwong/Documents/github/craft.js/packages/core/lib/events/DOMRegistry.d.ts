import { NodeId } from '../interfaces';
export declare class DOMRegistry {
    private dom;
    register(id: NodeId, element: HTMLElement): void;
    get(id: NodeId): HTMLElement;
    remove(id: NodeId): boolean;
    clear(): void;
}
