import React, { useMemo } from 'react';
import { EditorContext } from './EditorContext';
import { Events } from '../events';
import { EditorStoreImpl } from '../store';
/**
 * A React Component that provides the Editor context
 */
export const Editor = React.memo(({ children, enabled, ...config }) => {
    const store = useMemo(() => new EditorStoreImpl({
        state: {
            enabled: enabled !== undefined ? enabled : true,
        },
        ...(config || {}),
    }), [config, enabled]);
    if (!store) {
        return null;
    }
    return (React.createElement(EditorContext.Provider, { value: { store } },
        React.createElement(Events, null, children)));
});
//# sourceMappingURL=Editor.js.map