import React from 'react';
import { NodeQuery } from 'store';
export declare function connectNode<C>(collect?: (state: NodeQuery) => C): (WrappedComponent: React.ElementType<any>) => (props: any) => JSX.Element;
