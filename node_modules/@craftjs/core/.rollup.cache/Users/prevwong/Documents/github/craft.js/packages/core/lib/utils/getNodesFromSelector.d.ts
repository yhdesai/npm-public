import { NodeSelectorWrapper } from '../interfaces';
import { EditorStore } from '../store';
declare type config = {
    existOnly: boolean;
    idOnly: boolean;
};
export declare const getNodesFromSelector: (store: EditorStore, selector: string | string[] | import("../interfaces").Node | import("../interfaces").Node[], config?: Partial<config>) => NodeSelectorWrapper[];
export {};
